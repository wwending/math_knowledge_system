# --- Web Framework & API ---
fastapi>=0.100.0        # 核心 Web 框架
uvicorn[standard]>=0.20.0 # ASGI 服务器，用于运行 FastAPI
python-multipart>=0.0.6  # 必需，用于处理文件上传 (UploadFile)

# --- AI & OCR Engine (The Core) ---
pix2text>=0.3.0         # 核心库：理科公式 OCR (支持混合排版)
torch>=2.0.0            # 深度学习框架 (P2T 的底层依赖)
numpy>=1.24.0           # 数值计算
opencv-python-headless>=4.8.0 # 图像处理 (Headless版本适合服务器环境，不含GUI)
Pillow>=10.0.0          # 基础图像处理库

# --- Database & Models ---
sqlalchemy>=2.0.0       # ORM 框架，操作数据库
psycopg2-binary>=2.9.0  # PostgreSQL 数据库驱动 (Binary版免编译，开发友好)
alembic>=1.11.0         # 数据库版本迁移工具 (管理表结构变更)
pydantic>=2.0.0         # 数据验证与 Schema 定义
pydantic-settings>=2.0.0 # 环境变量配置管理

# --- Utilities ---
python-dotenv>=1.0.0    # 加载 .env 环境变量文件
loguru>=0.7.0           # 更好的日志记录库 (比 Python 自带的 logging 好用)
requests>=2.31.0        # 发送 HTTP 请求 (如果需要调用外部 API)

# --- Optional / Dev (可选) ---
# httpx                 # 如果需要异步 HTTP 客户端
# pytest                # 单元测试